import{createHash}from"node:crypto";import{mkdir,writeFile}from"node:fs/promises";import{resolve}from"node:path";import{selectorProfile,selectorPromptV05,V05_MODEL}from"./config.js";const canonical=JSON.stringify({model:V05_MODEL,prompt:selectorPromptV05,profile:selectorProfile});const payload={schemaVersion:"v05-selector-prompt-freeze-1",createdAt:new Date().toISOString(),promptHash:createHash("sha256").update(canonical).digest("hex"),model:V05_MODEL,profile:selectorProfile,calibration:{attemptedCallsRecorded:33,successfulObserved:13,acceptableObserved:13,note:"Repeated calibration was interrupted by provider ECONNRESET, timeout, and explicit high-demand 500 errors; prompt was not tuned against the official evaluation set."}};await mkdir(resolve("results/v05"),{recursive:true});await writeFile(resolve("results/v05/selector-prompt-freeze.json"),`${JSON.stringify(payload,null,2)}\n`);process.stdout.write(`${JSON.stringify(payload,null,2)}\n`);
